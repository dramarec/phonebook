import React from 'react';
import Container from '../../components/layout/container/Container';

const HomePage = () => {
    const styles = {
        textAlign: 'center',
    };
    return (
        <Container>
            <h2 style={styles}>Home Work Phonebook</h2>
        </Container>
    );
};

export default HomePage;
